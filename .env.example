# Public project identifier. Safe to expose to the browser.
NEXT_PUBLIC_SUPABASE_URL=https://xzkhhwlkiuvmyjngwbia.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only Supabase credential. NEVER prefix with NEXT_PUBLIC_ and never commit it.
SUPABASE_SERVICE_ROLE_KEY=

# Server-only OpenAI credential. NEVER prefix with NEXT_PUBLIC_ and never commit it.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
